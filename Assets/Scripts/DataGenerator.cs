using UnityEngine;
using System.Collections;

public class DataGenerator : MonoBehaviour {

	public int trial;
	public int block;
	public bool isGo;
	public float presTime;
	public float reactionTime;
	public bool hit;

	// Use this for initialization
	void Start () {
		/*CSV_Maker.Write (trial, block, isGo, presTime, reactionTime, hit);
		CSV_Maker.Write (trial, block, isGo, presTime, reactionTime, hit);
		CSV_Maker.Write (trial, block, isGo, presTime, reactionTime, hit);
		CSV_Maker.Write (trial, block, isGo, presTime, reactionTime, hit);
		CSV_Maker.Write (trial, block, isGo, presTime, reactionTime, hit);
		CSV_Maker.Write (trial, block, isGo, presTime, reactionTime, hit);
		CSV_Maker.Write (trial, block, isGo, presTime, reactionTime, hit);*/

	
	}

}
